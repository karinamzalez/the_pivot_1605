<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "World Wide Farmer's Market" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% if platform_admin? %>
      <%= render partial: 'shared/nave_bar', locals: { businesses: @businesses, categories: @categories } %>
    <% elsif business_admin? %>
      <%= render partial: 'shared/nave_bar', locals: { businesses: @businesses, categories: @categories } %>
    <% else %>
      <%= render partial: 'shared/nave_bar', locals: { businesses: @businesses, categories: @categories } %>
    <% end %>

  <section class="main">
    <% flash.each do |type, message| %>
      <div data-alert class="flash_<%= type %> alert-box round">
        <%= sanitize(message) %>
      </div>
    <% end %>

      <%= yield %>

  </section>

    <!-- <%= render partial: 'shared/footer' %> -->
  </body>
</html>

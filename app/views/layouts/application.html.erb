<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "World Wide Farmer's Market" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render partial: 'shared/nav_bar', locals: { businesses: @businesses, categories: @categories } %>
    <% if platform_admin? %>
      <%= render partial: 'shared/registered_user_nav_bar' %>
    <% elsif business_admin? %>
      <%= render partial: 'shared/registered_user_nav_bar' %>
    <% else %>
      <%= render partial: 'shared/registered_user_nav_bar' %>
    <% end %>

  <section class="main">
    <% flash.each do |type, message| %>
      <div data-alert class="flash_<%= type %> alert-box round">
        <%= sanitize(message) %>
      </div>
    <% end %>

      <%= yield %>

  </section>

    <!-- <%= render partial: 'shared/footer' %> -->
  </body>
</html>
